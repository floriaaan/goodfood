import { toStruct } from "@stock/lib/struct";
import { toGrpc } from "@stock/lib/transformer";

import { assert, describe, it } from "vitest";

describe("tests@goodfood/stock.transformer.toGrpc", function () {
  it("tests @goodfood/stock.transformer.toGrpc in expected situation", () => {
    // todo: add test
    assert(true);
  });
});
